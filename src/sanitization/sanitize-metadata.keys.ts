/** `Reflector` key `@LogAllowList` stores its shape under, via `SetMetadata`. */
export const ALLOW_LIST_METADATA_KEY = 'log-nest:allow-list'

/** `Reflector` key `@LogRedact` stores its redactor names under, via `SetMetadata`. */
export const REDACT_METADATA_KEY = 'log-nest:redact'
